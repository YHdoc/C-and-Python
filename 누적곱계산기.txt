#include <stdio.h>
int main(void)
{
    int i, sum = 1;
    for (i = 1; i <= 20; i += 1)	//1부터 20까지
    {
        if (i % 2 == 1)		//홀수일 경우에만 곱한다.
        {
            sum = sum * i;
        }
    }
    printf("%d\n", sum);
}

